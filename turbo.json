{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "inputs": ["src/**", "tsconfig.json", "package.json"]
    },
    "generate": {
      "cache": true,
      "inputs": ["src/tests/schema.prisma"],
      "outputs": ["src/tests/generated/**"]
    },
    "test": {
      "dependsOn": ["^build", "generate"],
      "cache": false,
      "inputs": ["src/**", "vitest.config.ts"]
    },
    "typecheck": {
      "dependsOn": ["^build", "generate"],
      "outputs": [],
      "inputs": ["src/**", "!src/**/generated/**", "tsconfig.json"]
    },
    "lint": {
      "outputs": [],
      "inputs": ["src/**", "biome.json"]
    }
  }
}
